---
import { Check, Copy, Play, RotateCcw } from 'lucide-astro';
import '../../styles/playground.css';

interface Props {
  /* The initial code shown in the editor. */
  code: string;
}

const { code } = Astro.props;
---

<div class='not-content'>
  <div class='doc-playground'>
    <div class='code-toolbar'>
      <button class='copy-btn playground-btn'>
        <span class='btn-text'>Copy</span>
        <span class='btn-icon copy-icon'><Copy size={16} /></span>
        <span class='btn-icon check-icon'><Check size={16} /></span>
      </button>
      <button class='run-btn playground-btn'>
        <span class='btn-text'>Run</span>
        <Play size={16} />
      </button>
      <button class='reset-btn playground-btn'>
        <span class='btn-text'>Reset</span>
        <RotateCcw size={16} />
      </button>
    </div>

    <div class='editor-mount' data-original={code}></div>

    <div class='output'>
      <div class='output-header'>Output</div>
      <iframe
        class='preview-frame'
        sandbox='allow-scripts allow-same-origin'
        title='Component preview'></iframe>
    </div>
  </div>
</div>
<script type='module'>
  import { initPlayground } from '../../playground/core';

  document.querySelectorAll('.doc-playground').forEach((el) => {
    if (el.dataset.playgroundReady === 'true') return;
    initPlayground(el);
    el.dataset.playgroundReady = 'true';
  });
</script>